name: Create Github Release

on:
  push:
    tags:
      - '*'

jobs:
  build:
    name: Create Release
    uses: ./.github/workflows/createReleaseGHJob.yml
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
