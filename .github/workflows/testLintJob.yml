name: Run Lint Tests

on:
  workflow_call: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/workflows/nodeSetupJob.yml
      - run: npm ci
      - run: npm run lint
